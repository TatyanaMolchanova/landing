var test = document.getElementById("test");

test.addEventListener("mouseover", function( event)) {
                      
    var capitalizedLink = link[0].toUpperCase() + link.slice(1).toLowerCase();
                      }